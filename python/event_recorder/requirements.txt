# Event Recorder & WordPress Publisher - Python Dependencies
# Phase 1 (Core Recording) + Phase 2 (Data Processing) Dependencies

# === Phase 1: Core Recording Infrastructure ===

# Database
# (sqlite3 is included in Python standard library)

# MQTT
paho-mqtt>=1.6.1,<2.0.0

# Configuration
PyYAML>=6.0,<7.0
python-dotenv>=1.0.0,<2.0.0

# === Phase 2: Data Processing & Plotting ===

# Plotting
matplotlib>=3.8.0,<4.0.0
numpy>=1.24.3,<2.0.0

# GPS route maps
#
# The route map PNG is matplotlib over the offline vector chart fetched from
# enchantee_racing (chart_map.py), because the boat has no internet and
# OpenStreetMap tiles are unreachable there. folium is optional and only writes
# the interactive OSM view embedded in the WordPress post; without it the chart
# map is still produced.
#
# selenium is gone, along with the chromium/chromium-driver apt packages in the
# Dockerfile. It existed solely to screenshot the folium HTML into a PNG, and
# nothing screenshots a browser now.
folium>=0.15.0,<1.0.0

# === Phase 3: Web Interface ===
Flask>=2.3.3,<3.0.0
gunicorn>=22.0.0,<23.0.0
Werkzeug>=2.3.0,<3.0.0

# === Phase 4: WordPress Integration ===
requests>=2.31.0,<3.0.0
python-dateutil>=2.8.2,<3.0.0  # For flexible date parsing

# === Development / Debugging ===
debugpy>=1.8.0,<2.0.0  # Remote debugging from VSCode

# === Development Dependencies (Optional) ===
# pytest>=7.4.0
# pytest-cov>=4.1.0
# black>=23.0.0
# flake8>=6.0.0
