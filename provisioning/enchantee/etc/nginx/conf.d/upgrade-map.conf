# Correct Connection header for proxied WebSocket upgrades.
# "upgrade" only when the client actually asked for it, "close" otherwise.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}
